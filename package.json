{
  "name": "final-project",
  "version": "1.0.0",
  "description": "**TARGET</H3>**\r >This application will target current casual carpool users in the Bay Area. Currently the casual carpool community utilizes predetermined pick-up spots where both drivers and riders meet and without previous knowledge of each other. \r We have identified an opportunity to provide both drivers and riders of the casual carpool a chance to determine who they ride with prior to their pick-up times.",
  "main": "server.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "node server.js",
    "start:dev": "concurrently \"nodemon --ignore 'client/*'\" \"npm run client\"",
    "client": "cd client && npm run start",
    "seed": "node scripts/seedDB.js",
    "install:dev": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/chihaby/final-project/issues"
  },
  "homepage": "https://github.com/chihaby/final-project#readme",
  "devDependencies": {
    "concurrently": "^4.1.2",
    "google-map-react": "^1.1.5",
    "nodemon": "^1.19.4"
  },
  "dependencies": {
    "@material-ui/core": "^3.9.4",
    "@pusher/chatkit-client": "^1.13.4",
    "@pusher/chatkit-server": "^1.1.0",
    "axios": "^0.18.1",
    "cookie": "^0.4.0",
    "cookie-parser": "^1.4.4",
    "cookieparser": "^0.1.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "faker": "^4.1.0",
    "if-env": "^1.0.4",
    "mongoose": "^5.8.7",
    "parser": "^0.1.4",
    "querystring": "^0.2.0",
    "react-google-maps": "^9.4.5",
    "request": "^2.88.0"
  }
}
